export const VERSION='1.0.0';export const KEY='conference_manager_rebuild_1_0_0';export const roles={admin:'مدير النظام',finance:'مسؤول المالية',committees:'مسؤول اللجان',viewer:'مشاهد'};export const rolePages={admin:['dashboard','members','expenses','revenues','committees','attendance','reports','settings','backup'],finance:['dashboard','members','expenses','revenues','reports','backup'],committees:['dashboard','members','committees','attendance','reports'],viewer:['dashboard','reports']};export const $=(s,r=document)=>r.querySelector(s);export const $$=(s,r=document)=>[...r.querySelectorAll(s)];export const uid=()=>crypto.randomUUID?crypto.randomUUID():'id'+Date.now()+Math.random().toString(36).slice(2);export const today=()=>new Date().toISOString().slice(0,10);export const num=v=>Number(String(v??'').replace(/[٠-٩]/g,d=>'٠١٢٣٤٥٦٧٨٩'.indexOf(d)).replace(',','.'))||0;export const money=v=>new Intl.NumberFormat('en-US',{maximumFractionDigits:2}).format(num(v))+' ج';export const base=()=>({conference:{name:'كنز ولا CANS',place:'بيت الإيمان - أسيوط',start_date:'2026-06-29',end_date:'2026-07-02',member_fee:0},users:[],members:[],expenses:[],revenues:[],committees:[],activities:[{id:uid(),name:'قداس 1'},{id:uid(),name:'محاضرة 1'}],attendance:{},audit:[],lastSave:null});export let state=load();export let currentUser=null;export function setUser(u){currentUser=u}export function load(){try{return {...base(),...(JSON.parse(localStorage.getItem(KEY)||'{}'))}}catch{return base()}}export function save(){state.lastSave=new Date().toISOString();localStorage.setItem(KEY,JSON.stringify(state));}export async function hash(x){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(String(x)));return [...new Uint8Array(b)].map(n=>n.toString(16).padStart(2,'0')).join('')}export function audit(action,details=''){state.audit.unshift({id:uid(),time:new Date().toLocaleString('ar-EG'),user:currentUser?.name||'system',action,details});state.audit=state.audit.slice(0,1000);save()}export function canPage(id){return currentUser&&rolePages[currentUser.role]?.includes(id)}export function toast(t){const e=$('#toast');e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),1800)}
